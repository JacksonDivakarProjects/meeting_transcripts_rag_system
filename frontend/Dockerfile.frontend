# Frontend - Vite React
FROM node:20-alpine

WORKDIR /app

# Install deps (cache optimized)
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]